---
dependencies:
  override:
    - pip install coveralls
    - pip install -q -r requirements.txt
    - npm install
    - tox --notest

  cache_directories:
    - .tox/
    - node_modules

test:
  override:
    - make test
  post:
    - coveralls
